const CONFIG = {
    API_URL: 'http://localhost:8000/api/v1',

    // UI Configuration
    UI: {
        MESSAGE_TIMEOUT: 5000,
        ANIMATION_DELAY: 100,
        CARD_MIN_HEIGHT: '300px'
    },

    // Default values
    DEFAULTS: {
        PAGINATION: {
            SKIP: 0,
            LIMIT: 100
        }
    }
};
